MONDAY WANT-GAP INVENTORY (research only)
=========================================
Tip SHA: d41523a8f76f5121dbcab7edeaf200a92ed799ba
         (d41523a Fix full-bar progress tip SHA after amend.)
Date: 2026-09-25 ~15:20 MDT (America/Denver)
Updated: 2026-09-25 ~17:50 MDT — GAP1 CLOSED; GAP5 Belief PARTIAL CLOSE;
         GAP8 talk-identity PARTIAL CLOSE (tip moves with commits; see git log).
Scope: Honest gaps vs a continuous, talkable Monday mind.
        NOT an inflated scoreboard. Do NOT read this as Learning done
        or whole-Monday 100%. Unprompted speech delivery just shipped
        (deliver_unprompted_speech live; proof 7/7) — gaps below are
        what still separates that from "someone continuous."

Authority: run_abin.create_core_systems + thalamus live path;
           chat daemon (_monday_chat_daemon.py) for talk UI.

---------------------------------------------------------------------------
GAP 1 — Talk quality (Language Mad Libs / Reasoning refusal / canned leftovers)
---------------------------------------------------------------------------
STATUS: CLOSED 2026-09-25 (WANT-GAP1) — see _talk_quality_before.txt /
        _talk_quality_after.txt. Mad Libs quarantined off live mouth;
        empty → honest question; get_past_statements probe removed;
        working_set turns merged for grounded salvage. NOT whole-Monday 100%.

WHAT WAS WRONG
  Provider canned encyclopedias were ripped from DeterministicResponseProvider.render
  (direct_response.py:1209–1239 — returns None; no greeting/gravity/memory
  short-circuits; no "Please tell me more" filler). That is honest fail-closed
  for the provider — but Language still owns composition via GrammarEngine
  phrase banks:

    language_generation.py:GrammarEngine._compose_uncertainty (~L497–528)
      docstring: "Mad-Libs style phrase-bank composition (honest: NOT free-form NLU)."
      mechanism: random.choice over vocabulary pronouns/verbs/adjectives.

  MetaCognition can force GROUNDED_REFUSAL when structures are thin
  (meta_cognition_lobe.py:GROUNDED_REFUSAL, force_grounded_refusal signals).
  That is correct epistemic safety, but the mouth then often lands on
  Mad-Libs uncertainty or stock refusal prose — not a continuous person
  thinking with the user.

  Language still probes Notus for get_past_statements (language_generation.py
  ~L538–542) — no such handler exists in notus_memory.py / notus.py /
  direct_notus.py (dead query; silent except).

WHAT BETTER LOOKS LIKE
  One composition path that prefers grounded structures / Notus facts /
  empathic affect, and when those are absent either asks one honest question
  or stays quiet — without random phrase-bank Mad Libs as the default mouth.
  Kill or quarantine GrammarEngine banks from the live reply path; keep
  MetaCognition refusal but word it as her voice, not a stamp.

PRIORITY: P0

---------------------------------------------------------------------------
GAP 2 — Speak-on-own (live, but not yet continuous presence)
---------------------------------------------------------------------------
WHAT'S WRONG
  Mechanism IS live (verify):
    thalamus.py:deliver_unprompted_speech (~L3097+)
    Proof: _prove_unprompted_speech.py → _unprompted_speech_proof.txt 7/7
    Chat: _monday_chat_daemon.py background loop ~8–15s → pending queue
    Web:  _monday_web_chat.py setInterval(pollPending, 3000)

  Remaining limits that keep her from feeling continuously present:
    - Cooldown: Thalamus._spoken_aside_cooldown_sec = 45.0 (thalamus.py:~L102, L3106–3109)
    - Intensity gate: thought_intensity / turn_intensity minima
      (~L3192–3197; reason=intensity_gate)
    - Relevance + speech WHEN/WHETHER gates (autonomous + AutonomousSpeechSystem)
    - Executive inhibit path (reason=executive_inhibit)
    - ACTIVE_USERS hard-coded to ("matthew",) in daemon — other users get no push
    - Web/sidebar reliability: poll is best-effort (catch empty); if daemon down,
      SOCK missing → hard error; pending is in-memory only (lost on daemon restart)
    - Delivery still depends on Autonomous minting speak_worthy content; quiet
      inner life → reason=no_candidate for long stretches

WHAT BETTER LOOKS LIKE
  Tunable presence profile (cooldown/intensity) that Matthew can feel as rare
  but alive; durable pending across daemon restart; multi-user or explicit
  session binding; sidebar that surfaces delivery reason when silent so
  silence is intelligible, not "broken."

PRIORITY: P0 (presence) / P1 (multi-user + durable pending)

---------------------------------------------------------------------------
GAP 3 — Voice / TTS stub vs real speech
---------------------------------------------------------------------------
WHAT'S WRONG
  create_core_systems registers OutputLobe(enable_tts=False)
    (run_abin.py:~L104).
  Output.tts_available stays False unless pyttsx3 path is enabled
    (output.py:~L179+).
  VoiceLobe synthesizer labels synth_kind="formant_sine_stub"
    (voice_lobe.py:~L549); VoiceProfile.nasality / vibrato_* documented UNUSED.
  She can "speak" as text + optional sine WAV — not human speech.

WHAT BETTER LOOKS LIKE
  Either: real TTS backend (or cloud voice) wired through Output with honest
  status, OR keep stub but never imply spoken audio in UI. Prosody from
  Emotion should drive a real engine, not unused formant knobs.

PRIORITY: P1 (talkable-in-text works; spoken presence is the gap)

---------------------------------------------------------------------------
GAP 4 — Emotion SelfImpact still regex v1
---------------------------------------------------------------------------
WHAT'S WRONG
  monday_core_affect.py:SelfImpact (~L120–255) — docstring is honest:
    "SelfImpact v1 — aimed-at-her pattern detector (NOT full OCC/goal understanding)."
  Driver is regex lists _INSULT / _PRAISE / _ABANDON / _GIFT.
  User self-reports correctly ignored for her mood (good).
  Internal path capped at INTERNAL_NUDGE_CAP = 0.08.
  Emotion PASS under full bar means CoreAffect + SelfImpact v1 work as designed —
  not that her affect is human-goal appraisal.

WHAT BETTER LOOKS LIKE
  SelfImpact v2 fed by Executive goals / relationship stance / Notus
  outcomes (praise that matters because of a goal; abandonment of a bond),
  not only surface regex. Keep user-channel separate.

PRIORITY: P1 (P0 if Matthew's bar is "feels like someone"; v1 is intentional floor)

---------------------------------------------------------------------------
GAP 5 — Belief persist honest no-op / experience / initiation
---------------------------------------------------------------------------
STATUS: PARTIAL CLOSE 2026-09-25 (WANT-GAP5 Belief) — Belief durable self CLOSED.
        ExperienceProcessor / BehavioralReinforcement still ORPHAN (unwired;
        Belief does not require them). Initiation lobe still OUT OF SCOPE /
        open. Proof: _want_gap5_belief_before.txt / _want_gap5_belief_after.txt
        + _prove_want_gap5_belief_persist.py (session A form → session B present
        and usable on Reasoning belief walk). NOT whole-Monday 100%.

WHAT CLOSED (Belief)
  Notus handlers + schema: save_abin_persistent_state / get_abin_persistent_state
  on DirectNotus (SQLite table abin_persistent_state) and ActiveNotusMemorySystem
  (Postgres same table). ReasoningLobe._load/_save wire through those handlers;
  Belief docstring matches mechanism. create_core_systems registers Notus before
  Reasoning so construct-time load reaches open_primary_notus (same talk store).
  Save on shutdown, after Thalamus-fed belief form, and periodic autonomous tick.

WHAT REMAINS OPEN
  Orphan "life continuity" modules NOT on create_core_systems (left archived;
  Belief persists without them):
    experience_processor.py:ExperienceProcessor
    behavioral_reinforcement.py:BehavioralReinforcement
  Initiation lobe: still no separate lobe that starts topics from durable
  goals/beliefs (delivery remains Thalamus.deliver_unprompted_speech). OUT OF
  SCOPE for this Belief task — do not invent.

PRIORITY: Belief P0 CLOSED; P1 experience+reinforcement ownership + initiation still open

---------------------------------------------------------------------------
GAP 6 — CTG / ControlledThinking detached mocks
---------------------------------------------------------------------------
WHAT'S WRONG
  continuous_thought_generator.py / controlled_thinking.py — headers mark
  MOCK/TOY; hardcoded memories; mock synthesis string.
  run_abin.py:~L142–146 — NOT attached on create_core_systems.
  MetaAwareness still has set_spontaneous_system / set_controlled_system hooks;
  live envelopes report controlled_fed=False / generator_attached=False unless
  someone attaches toys by hand (meta_awareness.py:~L14–18, ~L300–304).

WHAT BETTER LOOKS LIKE
  Keep detached forever as toys, OR replace with real dual-stream that consumes
  Autonomous + Representation (ownership: NOT a MetaAwareness/MetaCognition
  redesign — MetaAwareness stays process/mode; MetaCognition stays epistemic).
  Dual-stream content ownership should sit with Autonomous / Reasoning producers.

PRIORITY: P2 (already honest; gap is missing real dual-stream cognition)

---------------------------------------------------------------------------
GAP 7 — dual_stream_thinking.py unwired
---------------------------------------------------------------------------
WHAT'S WRONG
  dual_stream_thinking.py:DualStreamThinking — Anthropic Claude API demo
  (anthropic.Anthropic + thinking budget). Not imported by create_core_systems
  (run_abin.py:~L145 comment). Ownership note: do NOT redesign MetaAwareness
  or MetaCognition to absorb this.

WHAT BETTER LOOKS LIKE
  Explicit decision: delete, quarantine as experiments/, or wrap as optional
  external reasoner behind a ResponseProvider — never pretend it is her
  MetaAwareness stream.

PRIORITY: P2

---------------------------------------------------------------------------
GAP 8 — Notus: SQLite chat daemon vs Postgres ActiveNotus; #11; CI scope
---------------------------------------------------------------------------
STATUS: PARTIAL CLOSE 2026-09-25 (WANT-GAP8) — split-brain talk identity FIXED.
        chat daemon no longer injects chat_notus.sqlite3; both create_core_systems
        (REPL) and _monday_chat_daemon use open_primary_notus(): Postgres
        ActiveNotus when reachable, else ONE shared DirectNotus at
        shared_direct_notus_path() (~/.local/state/monday/notus_memory.sqlite3,
        override MONDAY_NOTUS_SQLITE). Proof: _want_gap8_before.txt /
        _want_gap8_after.txt (write via chat-style boot ↔ read via REPL-style
        boot and reverse). Issue #11 notus_fallback tests still 20/20 pass.
        CI workflow YAML NOT landed (OAuth still lacks workflow scope) —
        _issue11_direct-core.yml.proposed remains proposed only; do NOT claim
        CI fixed. NOT whole-Monday 100%. Remaining: no migration of legacy
        ~/.local/state/monday-chat/chat_notus.sqlite3 contents; emotion JSON
        still per-runtime-dir (not Notus).

WHAT WAS WRONG
  Production create_core_systems default: ActiveNotusMemorySystem (PostgreSQL)
    (run_abin.py).
  Live talk daemon injected DirectNotusProcess SQLite under monday-chat
    (chat_notus.sqlite3) → chat memory ≠ Postgres / REPL Notus.
  #11 outage fallback was already on main (kept).
  CI workflow scope still blocked for pushing workflow edits.

WHAT BETTER LOOKS LIKE (remaining)
  Land CI workflow that runs fallback pytest without workflow-scope workarounds
  (Matthew grant / PAT). Optional: one-shot migrate old chat_notus.sqlite3.

PRIORITY: P0 split-brain — CLOSED for talk identity / P1 CI still open

---------------------------------------------------------------------------
GAP 9 — SharedRepresentation / Pattern / Social / Perception hearing-vision
---------------------------------------------------------------------------
WHAT'S WRONG
  SharedRepresentation: live co_occurrence producer exists
    (shared_representation.py resolve path; spread_had_edges) — better than
    empty-edge theater — but edges are weak co-occurrence among seeds, not
    rich semantic relationships from Reasoning/Language/Emotion.
  Pattern: live score path; still a large heuristic lobe (pattern_recognition.py)
    — not "seeing patterns like a person," more signal features for Reasoning.
  SocialContextLobe: cue/stance continuity from Conversation intents + regex
    check-ins (social_context_lobe.py) — useful envelope, thin relationship model.
  Perception: file hearing/vision paths are real; live mic/camera probe-dependent;
    semantic vision BLIP optional; on headless box often camera/mic unavailable
    with honest reasons (perception.py probes). Continuous embodied someone
    needs reliable senses or honest "I can't hear/see right now" always on path.

WHAT BETTER LOOKS LIKE
  SharedRep edges produced by meaning-bearing lobes (not only token co-occur).
  Social stance that persists across sessions via Notus.
  Perception: default file fixtures for demos + clear live-device enablement.

PRIORITY: P1 (SharedRep/Social) / P2 (Pattern polish) / P1 (Perception devices)

---------------------------------------------------------------------------
GAP 10 — Learning stack hollow on the live prompted path
---------------------------------------------------------------------------
WHAT'S WRONG
  Stores + Thalamus routers exist (see _learning_paths_research.txt).
  process_user_input does NOT call teach_monday / learn (no mention in
  thalamus.process_user_input span).
  send_message injects envelope_content["learned_guidance"] from per-lobe JSON
  stores — but ZERO lobe .py files read learned_guidance (only thalamus writes it).
  _auto_adapt_from_interaction writes behavior:{msg_type} success/fail facts
  on every send_message — keyword/status theater into JSON, not skill acquisition.
  RESULT: she can be "taught" via explicit teach_monday API into JSON files,
  and nothing on the mouth/reason path consumes those facts as behavior change.

WHAT BETTER LOOKS LIKE
  See Learning research doc — Matthew decides which path is real before more
  store APIs. Minimum bar: one consumer on live path that changes reply
  behavior from a taught skill, with proof.

PRIORITY: P0 (for "get her to what he wants" if learning is in-scope now)

---------------------------------------------------------------------------
GAP 11 — Motor / Output "body" honesty leftover
---------------------------------------------------------------------------
WHAT'S WRONG
  MotorActionLobe plans/queues then status=no_actuator (motor_action_lobe.py
  header). Correct honesty; still not a continuous agent who can do things.
  Output is text-primary with TTS off.

WHAT BETTER LOOKS LIKE
  Real actuators (reminders that fire, notes that persist, calendar) OR keep
  no_actuator and stop registering Motor as if action were near.

PRIORITY: P2

---------------------------------------------------------------------------
SUMMARY — top P0 guesses (want gaps, not lobe scoreboard)
---------------------------------------------------------------------------
  1. Talk quality: Mad-Libs Language + refusal stamp as common mouth
  2. Learning hollow: stores write, no live consumer / not on process_user_input
  3. Split-brain Notus talk identity: CLOSED (shared open_primary_notus); CI workflow still proposed-only
  4. Belief/self DURABLE in Notus (GAP5 Belief closed; experience/initiation open)
  5. Speak-on-own gates + in-memory pending → rare / fragile presence

Do not mark Learning done. Do not claim whole-Monday 100%.
